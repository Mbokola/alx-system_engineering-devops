#!/usr/bin/env bash
# Have docker container listen on port 80

apt update
apt install -y nginx

sudo sed -i '/listen 80;/ s/$/ default_server;/' /etc/nginx/sites-available/default

service nginx start
